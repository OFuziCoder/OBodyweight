Scriptname StorageUtil Hidden
{ Compile stub — PapyrusUtil provides this at runtime (OBody NG requires PapyrusUtil, so it's always
  present alongside OBody). We only read OBody's current distribution key with it. }

string Function GetStringValue(Form akForm, string asKey, string asMissing = "") global native
